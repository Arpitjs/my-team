doctype html
head
  meta(charset='utf-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  title Cards Gallery
  link(rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css')
  link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/baguettebox.js/1.10.0/baguetteBox.min.css')
  link(rel='stylesheet' href='/css/cards-gallery.css')
  if user
    h1 i am the admin
    form.form.form-user-data      
      .form__group.form__photo-upload
        .form__group.right
          img.form__user-photo(src=`/img/programs/${prog.photos[0]}`, alt='User photo')
          input.form__upload(type='file', accept= 'image/*' id="photos" name="photos")
          label(for='photos') Choose new photo
          button.btn.btn--small.btn--green Save settings
    .line &nbsp;
    
    form.form.form-user-data2      
      .form__group.form__photo-upload2
        img.form__user-photo(src=`/img/arpit/${prog.abcd[0]}`, alt='User photo')
        input.form__upload(type='file', accept= 'image/*' id="ram" name="ram")
        label(for='abcd') Choose new photo
      .form__group.right
        button.btn.btn--small.btn--green Save settings
    .line &nbsp;

     form.form.form-user-data3     
      .form__group.form__photo-upload3
        img.form__user-photo(src=`/img/arpit/${prog.photos3[0]}`, alt='User photo')
        input.form__upload(type='file', accept= 'image/*' id="hero" name="hero")
        label(for='photos3') Choose new photo
      .form__group.right
        button.btn.btn--small.btn--green Save settings
    .line &nbsp;

- if(!user)
 

  h1 i am not admin !!  
    form.form.form-user-data      
      .form__group.form__photo-upload
        img.form__user-photo(src=`/img/arpit/${prog.photos3[0]}`, alt='User photo')
    .line &nbsp;
     form.form.form-user-data      
      .form__group.form__photo-upload
        img.form__user-photo(src=`/img/programs/${prog.photos[0]}`, alt='User photo')
    .line &nbsp;
     form.form.form-user-data      
      .form__group.form__photo-upload
        img.form__user-photo(src=`/img/arpit/${prog.abcd[0]}`, alt='User photo')
    .line &nbsp;
    
 

script(src='https://cdnjs.cloudflare.com/ajax/libs/baguettebox.js/1.10.0/baguetteBox.min.js')
script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/0.19.1/axios.min.js')
script(src="/js/upload.js")

